{
  "name": "glow-wars",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:minimal": "pnpm --filter @glow-wars/web-minimal dev",
    "dev:tanstack": "pnpm --filter @glow-wars/web-tanstack dev",
    "dev:backend": "pnpm --filter @glow-wars/convex dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "format": "prettier --write .",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "prettier": "^3.6.1",
    "typescript": "^5.8.3",
    "@types/node": "^24.0.4"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}